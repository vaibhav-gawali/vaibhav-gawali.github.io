title: "Vaibhav Gawali"
description: "Hardcore .NET Professional - Blog about .NET, C#, architecture, and software development"
author: "Vaibhav Gawali"
email: "vaibhav.gawali@gmail.com"
# Keep redirects relative so they work in local preview and across
# future custom domains (GitHub Pages, staging, etc.).
url: "https://vaibhav-gawali.github.io"
baseurl: ""

# Chirpy uses `site.social.name` and `site.social.links[0]` to render the
# author name when a post doesn't specify `author:`/`authors:` front matter.
social:
  name: "Vaibhav M. Gawali"
  links:
    - "https://vaibhav-gawali.github.io"

remote_theme: "cotes2020/jekyll-theme-chirpy"

markdown: kramdown
highlighter: rouge
timezone: UTC

permalink: /post/:slug/

# Use Chirpy layouts for everything
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "page"

paginate: 10

plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-remote-theme
  - jekyll-paginate
  - jekyll-archives
  - jekyll-include-cache
  - jekyll-redirect-from

# Chirpy sidebar renders navigation from the "tabs" collection.
# Your _tabs/ folder must be registered as a Jekyll collection.
collections:
  tabs:
    output: true

# Chirpy uses /tags/:name/ and /categories/:name/ routes.
# jekyll-archives needs explicit configuration for tag archives.
jekyll-archives:
  enabled:
    - categories
    - tags
  layouts:
    category: category
    tag: tag
  permalinks:
    tag: /tags/:name/
    category: /categories/:name/

exclude:
  - .venv/
  - .vs/
  - vendor/
  - node_modules/
  - Gemfile
  - Gemfile.lock
  - requirements.txt
